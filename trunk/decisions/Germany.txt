diplomatic_decisions = {

# Break the UK Naval Blockade	    
		break_blockade = {									
		potential = {
			tag = GER
			GER = {	war_with = ENG }									
			ENG = { has_country_flag = North_Sea_War_Zone } 			
			NOT = { GER = { has_country_flag = break_blockade } }
		}
		
		allow = { ai = no }								# AI is not to take this decision
			
		effect = {
			GER = { set_country_flag = break_blockade }
			GER = { country_event = 100518 } 
		}
		ai_will_do = { factor = 0 }
		}

# Zeppelin Bombing Campaign	   							
		zeppelin_bombing = {									
		potential = {
			tag = GER
			GER = {	war_with = ENG }
			NOT = { GER = { has_country_flag = Zeppelin_bombing } }			
			}
		
		allow = { total_we_bomb = 4 }							# This includes TAC on ground attack missions, calculated daily.
#				ENG = { strat_bomb_impact = 0.01 }			# Doesn't seem to trigger
			
		effect = {
			GER = { set_country_flag = Zeppelin_bombing }
			GER = { country_event = 100510 } 
		}
		ai_will_do = { factor = 1 }
		}

}